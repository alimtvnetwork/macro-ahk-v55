/**
 * A prompt file (markdown / JSON) shipped with the script and surfaced
 * via the prompts cache layer.
 *
 * All keys PascalCase per `mem://standards/pascalcase-json-keys`.
 */
export type PromptAsset = {
    readonly File: string;
};
