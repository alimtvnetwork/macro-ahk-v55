/**
 * IndexedDB constants for the prompt cache.
 */
export enum PromptCacheKey {
  DbName = 'marco_prompts_cache',
  Store = 'prompts',
  UiStore = 'ui_snapshots',
  JsonCopy = 'json_copy',
  HtmlCopy = 'html_copy',
  UiCache = 'dropdown_snapshot',
}
