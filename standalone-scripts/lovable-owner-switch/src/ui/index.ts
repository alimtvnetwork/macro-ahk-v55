/**
 * Owner Switch popup — public barrel.
 *
 * P7 surface: `mountPopup(host)` is the single entry the project
 * class will call once the SDK announces a popup root element.
 */

export { mountPopup } from "./popup-shell";
export * from "./popup-constants";
