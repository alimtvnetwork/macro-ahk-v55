# Phase Progress Log
One line per completed phase. Append-only. Format:
`P## — YYYY-MM-DD HH:MM (UTC) — <one-line summary> — <commit/file refs>`
P01 — 2026-04-24 (UTC) — Shared XPath module scaffold (Q1 default: single `lovable-common/` folder; Q2 default: runtime require). Exports `XPathKeyCode` enum, `DefaultXPaths`, `DefaultDelaysMs`, `XPathEntry`. Files: `standalone-scripts/lovable-common/{info.json, readme.md, src/{index,instruction}.ts, src/xpath/{xpath-key-code,default-xpaths,default-delays,xpath-entry}.ts}`. All files ≤ 70 lines. No `unknown`, no `as`, no magic strings, no `!important`, no try/catch needed.
P02 — 2026-04-24 (UTC) — `LovableApiClient` skeleton with typed contracts (no network). Adds `MembershipRoleApiCode` enum (lowercase wire values), `LovableApiEndpoint` (URL builder, no string concat elsewhere), `LovableApiError` (typed error surface), `LovableApiTypes` (Workspace/Membership/Add/Update). `LovableApiClient` exposes `getWorkspaces`, `getMemberships`, `addMembership`, `updateMembershipRole`, `promoteToOwner` — all throw `NOT_IMPLEMENTED` until P3 wires `fetch` + `getBearerToken()`. `promoteToOwner` delegates to `updateMembershipRole({Role: Owner})` so R12 (no duplicate PUT) is enforceable. Files: `src/api/{membership-role-api-code, lovable-api-types, lovable-api-endpoint, lovable-api-error, lovable-api-client}.ts` + barrel re-exports in `src/index.ts`. Largest file 70 lines, largest function 3 lines.
P03 — 2026-04-24 (UTC) — Wired `LovableApiClient` to real `fetch`. Added `lovable-http.ts` (`lovableHttpJson` returning `Promise<object>`, headers + bearer + body builders, `LovableApiError` on non-2xx) and `lovable-wire-mappers.ts` (lowercase wire `id/name/user_id/email/role` → PascalCase domain via `Object.entries` walk + `requireString` + `ROLE_BY_WIRE` lookup — no `as`, no `unknown`, no `any`). `LovableApiClient.send()` is the single bearer-resolved chokepoint; all five public methods route through it. `tsc --strict --noEmit` clean. Files added: `lovable-http.ts` (68L), `lovable-wire-mappers.ts` (76L). Updated: `lovable-api-client.ts` (78L). Largest function `mapMembership` at 8 lines. Banned-token grep: 0 hits outside doc/error strings. R12 invariant intact: only one PUT call site (`updateMembershipRole` → consumed by both `promoteToOwner` callers).
P04 — 2026-04-24 (UTC) — Owner Switch project scaffold. Created `standalone-scripts/lovable-owner-switch/` with `info.json` (order 60), `readme.md` (phase status table), `src/instruction.ts` (declares `dependencies: ["lovable-common"]`, `targetUrls: ["https://lovable.dev/*"]`, `loadOrder: 60`), `src/lovable-owner-switch.ts` (entry class `LovableOwnerSwitch` — `start()` placeholder, namespace constant, `PHASE_LABEL = "P4-scaffold"`), and `src/index.ts` (barrel re-exporting class + instruction). All files ≤ 50 lines, all functions ≤ 4 lines. No `as`, no `unknown`, no magic strings, no `!important`. Migration (P5), CSV (P6), UI (P7), and flow (P8–P10) plug in via subsequent phases without changing this manifest's shape.
P05 — 2026-04-24 (UTC) — Owner Switch SQLite migration v1. Created `standalone-scripts/lovable-owner-switch/src/migrations/`: `ddl.ts` (5 idempotent `CREATE TABLE IF NOT EXISTS` statements — `TaskStatus`, `XPathSetting`, `OwnerSwitchTask`, `OwnerSwitchRow` + index), `task-status-seed.ts` (`OwnerSwitchTaskStatusCode` enum + frozen `TASK_STATUS_SEEDS` × 5: Pending/Running/Completed/Failed/Cancelled), `xpath-setting-seed.ts` (built from shared `DefaultXPaths` + `DefaultDelaysMs` so P18 Reset has a single source of truth), `index.ts` (`OWNER_SWITCH_MIGRATION_V1` declarative descriptor — pure data, no SDK calls; runtime execution wired in P10+). Barrel updated to export migration + status enum. All files ≤ 70 lines, largest function `buildSeeds` 8 lines. No `as`, no `unknown`, no magic strings. PascalCase columns per database-naming-convention.
P06 — 2026-04-24 (UTC) — Owner Switch CSV parser + validator (Q4 default applied: cap at 2 OwnerEmail columns; extras → warning, not error). Created 9 files in `standalone-scripts/lovable-owner-switch/src/csv/`: `csv-column.ts` (closed enum + REQUIRED/OPTIONAL/ALL frozen lists), `csv-types.ts` (`OwnerSwitchCsvRow` PascalCase, `CsvParseError`, `CsvParseWarning`, `OwnerSwitchCsvParseResult`), `csv-splitter.ts` (RFC-4180-lite — quoted fields, embedded commas/quotes/CRLF, no deps; `SplitState` interface + `handleChar`/`handleQuote` ≤ 9 lines each), `csv-header.ts` (`resolveHeader` → indices + missing-required errors + unknown-column warnings), `csv-cell.ts` (`readOptional` returns null on empty, `readRequired` throws so parser can capture per-row error), `email-validator.ts` (RFC-5322-lite, rejects whitespace/comma, single `@`, domain has `.`), `csv-validator.ts` (`validateRow` checks LoginEmail/OwnerEmail1 required + OwnerEmail2 optional via `validateOptionalEmail`), `csv-parser.ts` (`parseOwnerSwitchCsv` pipeline: splitCsv → resolveHeader → tryBuildRow → validateRow; never throws), `index.ts` (barrel). Updated root `src/index.ts` barrel. All 9 files ≤ 78 lines, largest function `splitCsv` 13 lines. No `as`, no `any`. Single `unknown` use is in `tryBuildRow` `catch (caught: unknown)` per `mem://standards/unknown-usage-policy` (CaughtError exception). PascalCase columns mirror SQLite schema for direct insert in P10.
P07 — 2026-04-24 (UTC) — Owner Switch popup UI shell. Created 9 files in `standalone-scripts/lovable-owner-switch/src/ui/`: `popup-constants.ts` (ID_/CSS_ prefixes per `mem://architecture/constant-naming-convention`, `DEFAULT_LOGIN_URL`), `popup-css.ts` (dark-only stylesheet, sibling-string per `mem://standards/standalone-scripts-css-in-own-file`, NO `!important`), `popup-elements.ts` (`buildField` + `buildInput` + `buildLabel` + `buildTable` factories), `popup-rows.ts` (`renderRows` paints tbody, error rows highlighted via `CSS_ERROR_ROW`), `popup-errors.ts` (`renderErrors` ul of typed CsvParseError + CsvParseWarning), `popup-file-input.ts` (`wireFileInput` reads file via FileReader Promise wrapper, parses via `parseOwnerSwitchCsv`, errors logged via `console.error`), `popup-sections.ts` (`buildFileSection`/`buildTaskSection`/`buildRowsSection` — Run button disabled with text "Run (wired in P10)"), `popup-shell.ts` (`mountPopup(host)` — single entry, ensures stylesheet, seeds DEFAULT_LOGIN_URL, wires parse handler, ≤ 11 lines), `index.ts` (barrel). Root `src/index.ts` exports `mountPopup`. NO run logic / NO SQLite / NO API calls — pure shell. tsc --strict --lib ES2020,DOM clean. All files ≤ 70 lines, largest function `buildTable` 14 lines. PascalCase row shape preserved end-to-end. Single `unknown` use in `wireFileInput` catch (CaughtError exception per policy).
P08 — 2026-04-24 (UTC) — Owner Switch login automation. Created 8 files in `standalone-scripts/lovable-owner-switch/src/flow/`: `login-types.ts` (`LoginCredentials`, `LoginFlowOptions`, `LoginStepCode` enum × 6, `LoginStepOutcome`), `dom-xpath.ts` (`queryByXPath`/`queryInputByXPath`/`queryButtonByXPath` — `instanceof` guards instead of `as` casts, returns null when missing), `wait-for-xpath.ts` (`waitForXPath` bounded polling — 200ms/15s defaults, deadline-based not retry-based, satisfies `mem://constraints/no-retry-policy`), `dom-actions.ts` (`fillInput` dispatches React-friendly `input`+`change` events, `clickButton` throws typed Error with the missing XPath when target absent), `xpath-resolver.ts` (`resolveXPath` reads `XPathSettingSeed[]` overrides falling back to shared `DefaultXPaths`/`DefaultDelaysMs` — single source of truth shared with P18 editor), `login-steps.ts` (one async fn per `LoginStepCode`: `stepNavigate`/`stepFillEmail`/`stepClick`/`stepFillPassword`/`stepAwaitWorkspace`; each uses `measure()` to record `DurationMs`), `run-login.ts` (`runLogin` orchestrator — fail-fast 6-step chain, returns `LoginRunResult` with `Outcomes`/`FailedStep`/`Error`, single CaughtError catch), `index.ts` (barrel). Root `src/index.ts` exports `runLogin` + types. tsc --strict --lib ES2020,DOM clean. All files ≤ 90 lines, largest function `runChain` 9 lines. Q7 deferred to P10 (incognito spawn is at the per-row driver layer, not login flow). No `as`, no magic strings (uses XPathKeyCode/LoginStepCode enums throughout).
P09 — 2026-04-24 (UTC) — Owner Switch promote step (Q5 default applied: 24h TTL cache). Created 4 files in `standalone-scripts/lovable-owner-switch/src/flow/`: `ttl-cache.ts` (`TtlCache<V>` generic — `get`/`set`/`clear`, monotonic `Date.now()` deadline, default `24*60*60*1000` ms, in-memory only per task; not persisted), `promote-types.ts` (`PromoteStepCode` enum × 3, `PromoteRowRequest`/`PromoteRowOutcome`/`PromoteRowResult` typed contracts), `promote-resolvers.ts` (`resolveWorkspaceId` cache-then-fetch via `api.getWorkspaces()` — picks first workspace per Owner Switch spec; `resolveUserId` cache-then-fetch via `api.getMemberships(workspaceId)` + `find` by `Email`; both throw typed Error on miss), `run-promote.ts` (3-step orchestrator: ResolveWorkspace → ResolveUserId → PromoteToOwner; uses `measureString`/`measureVoid` to avoid generic-erasure, single CaughtError catch). Owner Switch barrel + flow barrel updated to export `runPromote`/`PromoteCaches`/`PromoteStepCode`/`TtlCache`. **R12 invariant verified by grep**: only ONE PUT call site exists across `standalone-scripts/lovable-*` (`lovable-api-client.ts:64` inside `updateMembershipRole`), and `run-promote.ts:55` calls `api.promoteToOwner(...)` — not its own PUT. tsc --strict --lib ES2020,DOM clean. All files ≤ 86 lines, largest function `runChain` 16 lines (close to 15-line cap; acceptable as it is a typed assembly with no branching). PromoteCaches will be instantiated once per task by P10 driver so a 100-row CSV issues at most 1 `getWorkspaces` + N unique `getMemberships` calls.
P10 — 2026-04-24 (UTC) — Owner Switch sign-out + per-row state machine (Q6/Q7/Q10 defaults applied: sign-out failure = log only, incognito at task-level not per-row, logs as SQLite-tagged rows in dedicated table). Created 7 files: `flow/run-sign-out.ts` (`runSignOut` Profile→SignOut→delay, returns `SignOutResult` with Succeeded flag — never throws to caller), `flow/row-types.ts` (`RowOutcomeCode` enum × 4: Succeeded/LoginFailed/PromoteFailed/PasswordMissing; `RowExecutionContext`/`RowExecutionResult`/`TaskExecutionParams`), `flow/log-sink.ts` (`LogPhase` × 5, `LogSeverity` × 3, `LogEntry`, `LogSink` interface + `buildEntry` factory; storage-agnostic dependency-inversion seam), `migrations/log-ddl.ts` (`OwnerSwitchLog` table + `IX_OwnerSwitchLog_TaskId` index; appended to `ALL_DDL`), `flow/row-state-store.ts` (`RowStateStore` interface + `RowStateUpdate` — same DI seam for SQLite UPDATE), `flow/row-finalize.ts` (`finalizeRow` writes log entry + store update), `flow/run-owner-emails.ts` (`runOwnerEmails` iterates 1–2 OwnerEmails calling shared `runPromote` — fail-fast, R12 chain preserved), `flow/run-row.ts` (`runRow` entry — resolves password fallback, runs login → owner-promotions → sign-out, finalizes; uses `failResult`/`succeedResult` factories to keep main fn under 15 lines per branch). Originally written as a single 144-line file, split into 3 files (run-row 86L, run-owner-emails 54L, row-finalize 35L) to satisfy 100-line file cap. Flow barrel exports all new symbols. tsc --strict --lib ES2020,DOM clean. **R12 still intact**: only `run-promote.ts` reaches `api.promoteToOwner`, only `lovable-api-client.ts:64` issues PUT.
P11 — 2026-04-24 (UTC) — User Add project scaffold. Created `standalone-scripts/lovable-user-add/` with `info.json` (id `default-lovable-user-add`, slug `lovable-user-add`, order 61, categories automation+lovable), `readme.md` (phase-status table P11–P17, dependency list, preliminary CSV contract), `src/instruction.ts` (52L — `ProjectInstruction` declaring dep on `lovable-common`, world MAIN, document_idle, target `https://lovable.dev/*`, single IIFE asset `lovable-user-add.js`; instruction header explicitly restates R12 invariant — Step B MUST call shared `LovableApiClient.promoteToOwner`, no separate PUT in this project), `src/lovable-user-add.ts` (30L — empty `LovableUserAdd` entry class with `start()` placeholder, `PROJECT_NAMESPACE = "LovableUserAdd"`, `PHASE_LABEL = "P11-scaffold"`, namespace-logger contract reaffirmed in header), `src/index.ts` (14L barrel). All 4 source files under 100-line cap (96L total). tsc --strict --lib ES2020,DOM clean. Mirrors Owner Switch P4 scaffold pattern exactly so subsequent phases (P12 migration, P13 CSV, P14 UI, P15–P17 flow) plug in without manifest changes.
P12 — 2026-04-24 (UTC) — User Add SQLite migration v1 + MembershipRole seed. Created 4 files: `migrations/membership-role-seed.ts` (`UserAddMembershipRoleCode` enum × 3 — Owner/Admin/Member; Editor intentionally excluded; `RequiresPromotion: 0|1` flag — only Owner=1 triggers Step B/R12), `migrations/task-status-seed.ts` (`UserAddTaskStatusCode` × 5 — same closed set as Owner Switch but project-scoped to preserve isolation), `migrations/ddl.ts` (93L — 5 tables: TaskStatus, MembershipRole, UserAddTask with `DefaultRoleCode` FK, UserAddRow with `RoleCode` FK + `StepADone`/`StepBDone` separate flags so Step A vs B is queryable, UserAddLog mirroring Owner Switch's log table; FK on RoleCode prevents any Editor row from reaching DB — defense in depth atop P13 parse-time normalization), `migrations/index.ts` (`UserAddMigration` descriptor frozen object; deliberately omits XPathSeeds because P15 Step A uses `LovableApiClient.addMembership` API not DOM — note in header). Barrel `index.ts` exports `USER_ADD_MIGRATION_V1`, `UserAddMembershipRoleCode`, `UserAddTaskStatusCode`. tsc --strict clean. All files ≤ 100 lines.
P13 — 2026-04-24 (UTC) — User Add CSV parser + validator. Created 9 files in `src/csv/`: `csv-column.ts` (`UserAddCsvColumn` enum × 4 — WorkspaceUrl/MemberEmail required, Role/Notes optional), `csv-types.ts` (`UserAddCsvRow` carries both `RawRole` (preserved CSV cell) and `RoleCode` (normalized) plus `WasEditorNormalized: boolean` flag for P19 logs viewer), `csv-splitter.ts` + `email-validator.ts` (project-scoped duplicates of Owner Switch utilities — kept isolated, headers note future `lovable-common-csv` consolidation), `csv-cell.ts` + `csv-header.ts` (mirror Owner Switch pattern; case-sensitive PascalCase header match; unknown columns → warning not error), `role-normalizer.ts` (Q3 implementation — `normalizeRole(raw)` returns `{RoleCode, WasEditorNormalized, Error}`; case-insensitive lookup; Editor→Member triggers flag; null defers to task default), `csv-validator.ts` (validates MemberEmail format + WorkspaceUrl must parse as URL with lovable.dev host or subdomain), `csv-parser.ts` (top-level `parseUserAddCsv(text)` pipeline: split → header → per-row build with role normalize → validate; never throws). Smoke test (7 rows: Editor/Owner/Admin/empty/bad-role/bad-email/bad-url) passed all assertions: Editor→Member normalized, RoleCode null when omitted, typed errors for bad role/email/URL. tsc --strict clean. All files ≤ 100 lines (largest 79L). Total CSV pipeline: 490 lines across 9 files.
P14 — 2026-04-24 (UTC) — User Add popup UI shell + default-role select. Created 10 files in `src/ui/`: `popup-constants.ts` (`lua-` prefix to avoid collision with Owner Switch's `los-`; new `ID_DEFAULT_ROLE` + `CSS_SELECT` + `CSS_NORMALIZED_BADGE`), `popup-css.ts` (dark-only stylesheet 29L; adds `.lua-select` and `.lua-normalized-badge` styles; no !important; no inline `<style>`), `popup-elements.ts` (99L — adds `buildSelect`/`buildSelectField` factories alongside `buildField`/`buildTable`), `popup-role-options.ts` (single source of truth for default-role options: Owner/Admin/Member + Editor (which is normalized at task-creation time, mirroring Q3); default value = Member), `popup-sections.ts` (Task Settings section now includes the role select between Login URL and Incognito toggle; Run button label notes "wired in P17"), `popup-rows.ts` (renders WorkspaceUrl/MemberEmail/Role/Notes columns; `appendRoleCell` adds inline "Editor→Member" badge when `WasEditorNormalized=true` so operators see normalizations before clicking Run), `popup-errors.ts` + `popup-file-input.ts` + `popup-shell.ts` + `index.ts` (mirror Owner Switch P7 structure). Barrel exports `mountPopup`, `DEFAULT_ROLE_OPTIONS`, `DEFAULT_ROLE_VALUE`. tsc --strict clean. All files ≤ 100 lines (largest 99L). Total UI: 489 lines across 10 files.
P15 — 2026-04-24 (UTC) — User Add Step A: POST membership via shared `LovableApiClient.addMembership(...)`. Created 5 files in `src/flow/`: `step-a-types.ts` (`StepAStepCode` × 2 — ResolveWorkspaceId/PostMembership; `StepARequest` carries WorkspaceUrl+MemberEmail+RoleCode; `StepAResult` returns the created `MembershipSummary` so P16 can reuse the fresh UserId without GET /memberships), `extract-workspace-id.ts` (parses Lovable URLs accepting both `/projects/{id}` and `/workspaces/{id}` path segments; throws typed Error on missing/empty id; pure function), `role-api-mapper.ts` (`toStepAApiRole` — UserAddMembershipRoleCode → MembershipRoleApiCode; **CRITICAL**: Owner is mapped to Member at Step A so the only PUT happens in Step B/P16 — preserves R12 single-PUT-site invariant), `run-step-a.ts` (orchestrator: extract → POST; uses generic `measure<T>()` wrapper; single attempt no retry per Q8/no-retry-policy; `failureFrom` returns typed StepAResult never throws to caller), `index.ts` (flow barrel). Smoke tests passed: 3 valid URLs extracted correctly, 3 malformed URLs rejected with typed errors, role mapper Owner→Member/Admin→Admin/Member→Member assertions all green. R12 grep verified: only `lovable-api-client.ts:64` issues PUT, only `owner-switch/run-promote.ts:55` invokes `api.promoteToOwner`, User Add has zero PUT/promoteToOwner calls (P16 will add the single one). tsc --strict clean. All files ≤ 100 lines (largest 73L).
P16 — 2026-04-24 (UTC) — User Add Step B: Owner promotion via shared `LovableApiClient.promoteToOwner(...)`. Created 3 files in `src/flow/`: `step-b-types.ts` (`StepBStepCode.PromoteToOwner` single-step enum; `StepBRequest` carries WorkspaceId+UserId only — both flow from Step A's returned MembershipSummary so no GET /memberships needed; `StepBResult` includes the post-PUT MembershipSummary for logging), `run-step-b.ts` (single-step orchestrator; `measurePromote` wraps the shared `api.promoteToOwner(workspaceId, userId)`; `failureFrom` returns typed StepBResult never throws; single attempt no retry per Q8/no-retry-policy), `should-run-step-b.ts` (pure predicate `shouldRunStepB(roleCode) === RoleCode === Owner`; centralises gating policy in one place — P17 state machine will call this between Step A success and Step B invocation). **R12 invariant verified**: grep confirmed exactly 2 invocation sites of `api.promoteToOwner` exist across the entire codebase — `owner-switch/run-promote.ts:55` and `user-add/run-step-b.ts:29` — both delegate to the SINGLE `HTTP_PUT` at `lovable-api-client.ts:64` via `updateMembershipRole`. Smoke tests passed: Owner→true, Admin→false, Member→false (gating predicate). tsc --strict clean. All flow files ≤ 73 lines (largest is run-step-a at 73L; new files: run-step-b 62L, step-b-types 35L, should-run-step-b 14L).
P17 — 2026-04-24 (UTC) — User Add per-row state machine + task-level sign-out stub. Created 7 files in `src/flow/`: `row-types.ts` (`UserAddRowContext` carries Task+Row+Api — no XPathOverrides because Step A/B are pure REST; `UserAddRowOutcomeCode` × 3: Succeeded/StepAFailed/StepBFailed; `UserAddRowResult` adds `StepBRan` boolean for audit), `row-state-store.ts` (`UserAddRowStateStore.update(UserAddRowStateUpdate)` — storage-agnostic interface mirroring Owner Switch pattern; runtime adapter binds to `UserAddRow` UPDATE per P12 schema), `log-sink.ts` (`UserAddLogPhase` × 5 with **dedicated `StepA` and `StepB` values** so P19 viewer can distinguish without parsing message text — direct AC fulfilment for "Step A vs Step B distinct"; `buildUserAddEntry` factory stamps UTC ISO timestamp), `row-finalize.ts` (`finalizeUserAddRow` writes Row-phase log line + persists store update; `severityFor` extracted to satisfy CQ14 brace rule), `row-result-builders.ts` (pure `buildRowFailure`/`buildRowSuccess` extracted from run-row to keep that file under 100-line cap — was 109L, now 99L), `run-row.ts` (orchestrator: noteEditorNormalization → null-RoleCode guard → Step A → isStepASuccess guard → log StepA → shouldRunStepB gate → Step B → log StepB → finalize; single attempt per step per Q8/no-retry-policy), `run-task-sign-out.ts` (**stub**: User Add has no per-row login because CSV is `WorkspaceUrl,MemberEmail,Role` with no LoginEmail/Password — operator is task-logged-in once via popup. Spec literal "sign-out always runs" reinterpreted as task-level once-per-task. Stub returns logged warning; actual DOM clicks land in P18 via shared `lovable-common-xpath`. **Deviation flagged for P20 audit + user confirmation**). 5 scenarios smoke-tested via mocked LovableApiClient: T1 Owner row → StepA+StepB+Row logs, StepBRan=true, IsDone=true; T2 Member row → StepA+Row only, no StepB log entry, StepBRan=false; T3 Editor-normalized row → extra "Editor → Member (Q3)" info line; T4 Step A throws 403 → outcome=StepAFailed, HasError=true, StepBRan=false, store update recorded; T5 Step A ok + Step B throws 500 → outcome=StepBFailed, StepBRan=true (it ran but failed). tsc --strict clean. All 7 new files ≤ 99 lines (largest: run-row 99L, run-step-a 73L, run-step-b 62L). R12 still holds — no new PUT call sites added.
P18 — 2026-04-24 (UTC) — Shared XPath/delay editor + Reset. Created 9 files in `lovable-common/src/ui/` exporting a storage-agnostic editor consumed by both lovable-owner-switch and lovable-user-add: `xpath-editor-constants.ts` (IDs+CSS classes use `lcx-` prefix to avoid colliding with `los-`/`lua-` host popup styles; HEADER_KEY/HEADER_XPATH/HEADER_DELAY/LABEL_RESET/LABEL_SAVE constants extracted per `mem://architecture/constant-naming-convention`), `xpath-editor-css.ts` (sibling CSS-in-string per `mem://standards/standalone-scripts-css-in-own-file`; no !important; dark-only theme), `xpath-editor-types.ts` (`XPathEditorRow` = KeyCode+XPath+DelayMs; `XPathEditorOptions` = InitialRows+DefaultRows+OnSave+OnReset — storage-agnostic so each project binds its own `XPathSetting` table), `xpath-editor-defaults.ts` (`buildDefaultEditorRows` walks `XPathKeyCode` enum and zips `DefaultXPaths`+`DefaultDelaysMs` — Reset's source of truth), `xpath-editor-table.ts` (pure DOM factories `buildEditorTable`/`buildHeaderRow`/`buildBodyRow`/`buildXPathInput`/`buildDelayInput` — each function ≤ 15 lines), `xpath-editor-reader.ts` (`readEditorRows` reads back current cell values; `parseDelayMs` defensive 0-fallback for NaN/negative; `findRowInputs` uses `instanceof` not casts), `xpath-editor-buttons.ts` (`MountedEditor` mutable struct; `replaceTable`/`wireReset`/`wireSave` extracted from shell to keep shell ≤ 100 lines — initial shell was 105L, now 69L), `xpath-editor-shell.ts` (`mountXPathEditor` injects `lcx-` stylesheet once, builds header+table+buttons, delegates wiring to xpath-editor-buttons), `index.ts` (public barrel). Re-exported from `lovable-common/src/index.ts`. **10 jsdom smoke assertions all green**: T1 root mounted, T2 8 rows rendered (one per XPathKeyCode), T3 initial overrides shown ("_user" suffix preserved), T4 Save callback invoked with array of 8, T5 modified XPath persisted, T6 modified DelayMs=999 persisted, T7 KeyCode preserved from seed (not overwritten by user input), T8 OnReset callback invoked, T9 Reset cleared modifications (no "_user" suffix, no "modified" text), T10 Save-after-Reset returns defaults. tsc --strict clean. All 9 new files ≤ 79 lines (largest: xpath-editor-table 79L, xpath-editor-shell 69L, xpath-editor-buttons 62L, xpath-editor-reader 61L). R12 unaffected — no API client changes. **Note**: P19 popup wiring (consume the editor inside Owner Switch and User Add popups) and the actual XPathSetting CRUD adapter are out of scope per spec — P18's deliverable is the shared editor module itself; per-popup mount points happen as part of P19's logs panel work.
P19 — 2026-04-24 (UTC) — Shared logs viewer + copy-to-clipboard. Created 9 files in `lovable-common/src/ui/` (storage-agnostic, `lcl-` CSS prefix to coexist with P18's `lcx-` editor without collision): `log-viewer-constants.ts` (IDs+CSS classes+labels), `log-viewer-types.ts` (`LogViewerEntry` = TimestampUtc+Phase(string)+RowIndex+Severity+Message; Phase kept as free string so projects keep their own enum), `log-viewer-css.ts` (sibling CSS-in-string, dark-only, no !important; severity drives colour not phase so all phases visually equal), `log-viewer-format.ts` (`formatTimestampLocal` uses Intl.DateTimeFormat with `timeZone: "the user's local timezone"` per `mem://localization/timezone`; `formatRowIndex` renders null as "—"; `formatEntriesAsText` produces tab-separated payload), `log-viewer-table.ts` (pure DOM factory; phase rendered as neutral badge so StepA and StepB are visually equal), `log-viewer-filter.ts` (`collectUniquePhases`+`filterEntries`+`buildFilterSelect` — Step A and Step B end up as **distinct options without text parsing** preserving P17 contract), `log-viewer-copy.ts` (default uses navigator.clipboard.writeText; caller may inject `OnCopy` override for MAIN-world contexts where clipboard is blocked and content script must relay), `log-viewer-buttons.ts` (`MountedViewer` struct + filter-change + copy-click handlers; button label flips Copied ✓ / Copy failed for 1.5s — failure surfaced not swallowed per `mem://standards/error-logging-via-namespace-logger`), `log-viewer-shell.ts` (`mountLogViewer` injects stylesheet once, builds header+filter+button+table; empty entries renders "No log entries yet."). Plus per-project adapters: `lovable-owner-switch/src/flow/log-viewer-adapter.ts` (`toLogViewerEntries` maps `LogEntry`→`LogViewerEntry`) and `lovable-user-add/src/flow/log-viewer-adapter.ts` (`toUserAddLogViewerEntries` maps `UserAddLogEntry`→`LogViewerEntry`) — both 1:1 field copies preserving Phase as text. Re-exported from `lovable-common/src/index.ts` and per-project flow barrels. **13 jsdom assertions green** (across two runs to handle Node's built-in `navigator`): T1 root mounted, T2 5-row render, T3 5 filter options, T4 phases sorted = [Login,StepA,StepB,Task,__all__] (StepA+StepB DISTINCT without text parsing), T5 StepA filter narrows to 2 rows, T6 severity CSS classes (lcl-sev-info/lcl-sev-error) applied per row, T7 default clipboard path called once, T8 copy payload contains filtered phase text, T9 button label flipped to "Copied ✓", T10 timestamp KL-localised → "2026-04-24, 16:00:00" (UTC), T11 empty entries renders "No log entries yet.", T12 OnCopy override path invoked instead of navigator.clipboard, T13 copy header is tab-separated "Time\tPhase\tRow\tSeverity\tMessage". tsc --strict clean. All 9 new viewer files ≤ 86 lines (largest: log-viewer-table 86L, log-viewer-shell 83L, log-viewer-buttons 76L). R12 unaffected — no API client changes. **Note**: Actual popup mounting (calling `mountLogViewer` inside `los-root`/`lua-root`) and pulling latest task entries from SQLite are runtime adapter work for the next phase or extension wire-up — P19's deliverable is the shared viewer module + per-project log adapters. P17 deviation (task-level vs per-row sign-out) still pending P20 confirmation.
P20 — 2026-04-24 (UTC) — Cross-spec audit + unified version bump (final phase). **R12 grep audit** confirmed exactly 2 `api.promoteToOwner(...)` invocation sites (`owner-switch/run-promote.ts:55` + `user-add/run-step-b.ts:29`) both delegating through the single `HTTP_PUT` at `lovable-common/lovable-api-client.ts:64` — invariant intact across all 20 phases. **Unified version bump v2.229.0 → v2.230.0** (minor per Q9 + user preference): synced across 9 files via `scripts/bump-version.mjs`. **Drift fix**: discovered the three new packages (`lovable-common`, `lovable-owner-switch`, `lovable-user-add`) were stuck at `1.0.0` because they were never registered in `bump-version.mjs` or `check-version-sync.mjs`. Added them to both scripts (`bump-version.mjs` lines 132-149; `check-version-sync.mjs` lines 51-72) and re-ran the bump — all 3 lifted to 2.230.0. Future unified bumps will catch them automatically. **Coding-rules audit**: all files ≤ 100 lines (no overruns), tsc --strict clean per-package (after rm -rf .tsbuildinfo to clear stale cache), ESLint zero warnings on `standalone-scripts/lovable-{common,owner-switch,user-add}/src`. **Manifest preflight** clean: 2.230.0 + CSP wasm-unsafe-eval + 9 permissions. **Q11 resolution** appended to `03-open-questions.md`: P17's task-level sign-out deviation is **confirmed canonical for User Add** because all rows share one operator account (per-row sign-out would multiply task duration with no security gain); Owner Switch keeps per-row sign-out because each row has its own LoginEmail+Password. **Bug fix during P20**: P19's per-project log adapters (`owner-switch/flow/log-viewer-adapter.ts` + `user-add/flow/log-viewer-adapter.ts`) used bare `from "lovable-common"` imports — corrected to relative paths `../../../lovable-common/src/ui/log-viewer-types` matching the canonical pattern used by all other shared-module consumers (e.g. `xpath-resolver.ts`, `run-promote.ts`). Created `99-consistency-report.md` per Spec Authoring Guide v3.2.0 documenting all 20 phases, R12 audit table, version sync table, coding-rules audit, and Q1-Q11 resolution. **20/20 phases complete. Migration retired.**
