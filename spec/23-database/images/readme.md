# Rendered DB Diagrams

PNGs in this folder are generated from `../diagrams/*.mmd` by:

```bash
node scripts/render-db-diagrams.mjs
```

Do not edit images by hand — re-run the script after editing the `.mmd`
sources. See `mem://style/diagram-visual-standards` for visual rules.
