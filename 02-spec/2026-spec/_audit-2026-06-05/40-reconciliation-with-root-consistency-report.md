# Reconciliation with per-folder consistency reports

The root and per-folder `99-consistency-report.md` files audit folder **structure** (presence of required files, anchors, scope). This generated audit measures **content quality** and blind-AI implementability. They are complementary, not contradictory; this page is the single point that pins them together.

## Where they agree

- `2026-spec/` is structurally present.
- The active 2026 spec folders expose README or overview entry points.
- Files marked ✅ in per-folder consistency reports also clear every content-quality gate listed in `30-remediation-backlog.md`.

## Where this audit extends the per-folder reports

1. Per-file `## Acceptance` presence (`check-acceptance.mjs`).
2. Dangling inline and reference-style relative links (`check-dangling-links.mjs`).
3. Numeric-constant source-of-truth binding (`check-constant-divergence.mjs`, `check-must-constants.mjs`).
4. Pitfalls / counter-examples coverage (`check-pitfalls.mjs`).
5. MUST/SHALL → `mem://` owner traceability (`check-must-memory-refs.mjs`).
6. Blind-AI implementability score per file (`audit-scan.py`).

## Per-folder cross-reference

| Folder | Structure | Content Quality (blind-AI) | Per-folder consistency report |
| --- | --- | --- | --- |
| `2026-spec/01-prompt-spec` | ✅ | 🟢 100 | — (none) |
| `2026-spec/02-ci-cd-spec-for-chrome-extensions` | ✅ | 🟢 100 | [`spec/2026-spec/02-ci-cd-spec-for-chrome-extensions/99-consistency-report.md`](../02-ci-cd-spec-for-chrome-extensions/99-consistency-report.md) |
| `2026-spec/03-chrome-ext-features` | ✅ | 🟢 100 | — (none) |
| `2026-spec/03-db-and-sqlite-integration-with-chrome-extension` | ✅ | 🟢 100 | — (none) |

## Gaps

The following folders do **not** yet ship a per-folder `99-consistency-report.md` and rely on this audit alone for structural sign-off:

- `2026-spec/01-prompt-spec`
- `2026-spec/03-chrome-ext-features`
- `2026-spec/03-db-and-sqlite-integration-with-chrome-extension`

The addendum is not auto-applied; per-folder consistency-report ownership remains with each folder maintainer. This page is generated by `scripts/audit/render-reports.mjs` and MUST be regenerated rather than hand-edited.
