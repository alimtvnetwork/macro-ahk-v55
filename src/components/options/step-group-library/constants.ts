/**
 * Shared constants for the Step Group Library sub-components.
 *
 * Extracted from `StepGroupLibraryPanel.tsx` as part of Plan 24 / Step 3
 * to keep the mime types and repeated UI copy in a single canonical
 * source (fixes `sonarjs/no-duplicate-string` for the "Select a group
 * first" tooltip that appeared four times).
 */

export const DRAG_MIME = "application/x-marco-step-group";
export const STEP_DRAG_MIME = "application/x-marco-step";
export const SELECT_GROUP_FIRST_TOOLTIP = "Select a group first";
