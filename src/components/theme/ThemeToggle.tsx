/**
 * ThemeToggle — DEPRECATED
 *
 * Dark-only mode enforced. This component is intentionally empty.
 * Kept as a stub to avoid breaking any lingering imports.
 */
export const ThemeToggle = () => null;
