The project employs a platform adapter pattern to abstract environment-specific APIs. Core operations, including message passing (via 'getPlatform().sendMessage()') and active tab retrieval, must use this abstraction to ensure compatibility across the production extension and development preview environments. Remaining direct 'chrome.runtime' calls are restricted to areas where platform abstraction is not applicable, such as the adapter implementation itself or low-level relay scripts.
