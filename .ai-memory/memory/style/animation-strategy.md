Animation and micro-interactions (e.g., hover scales, active presses, view transitions) are implemented strictly using Tailwind CSS utility classes and standard CSS keyframes. The project explicitly avoids external animation libraries like Framer Motion to minimize bundle size overhead (~30KB), prioritizing performance and maintaining a zero-dependency animation strategy within the design system.
