The deployment script ('browser-deploy.ps1') includes a 'Test-ExtensionInProfile' guard that detects if the current 'dist' folder is the one actually loaded in the active Chrome profile. If a path mismatch is detected, the script provides specific instructions to remove stale copies and manually reload the extension from the correct path at 'chrome://extensions' to prevent version and script synchronization errors.
