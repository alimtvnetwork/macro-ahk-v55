The project maintains a state of zero ESLint warnings and zero errors across the entire 'src/' directory and 'standalone-scripts/macro-controller/'. This was achieved by eliminating over 500 'no-explicit-any' violations, refactoring large components into modular units, and resolving all pre-existing test failures (1080/1080 tests passing as of v2.137.0). The '@typescript-eslint/no-restricted-types' rule is disabled to permit the centralized 'CaughtError' pattern while enforcing a strict 'No Explicit Unknown' policy via architectural standards. All files in the macro-controller are verified to be ESLint-clean.
