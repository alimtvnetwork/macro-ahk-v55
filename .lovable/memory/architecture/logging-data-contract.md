There is a data contract discrepancy in the logging system: the SQLite backend returns PascalCase keys (e.g., 'Timestamp', 'Level'), while the frontend UI components expect camelCase or lowercase keys. All log consumers, UI renderers, and exporters must implement normalization or defensively handle both casing styles to prevent data rendering failures (e.g., blank lines in the Activity Log or Session Reports).
