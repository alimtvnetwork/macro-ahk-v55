# Plan 10 — Fix all Vitest errors and `act(...)` warnings

Slug: plan-10
Steps: 10
Status: completed
Created: 2026-07-20

## Context

CI log (`user-uploads://file-54`) shows one hard test failure in
`js-step-diagnostics.test.ts` (em-dash vs comma in `formatFailureReport`
output) and repeat `act(...)` warnings driven by async mount effects and
real-timer state clears. A first-pass hotfix landed for two files this
turn; this plan sweeps the remaining offenders repo-wide, standardizes a
`flushEffects` helper, and ratchets a CI gate so `act(...)` warnings and
em-dash assertions never regress.

Related:
- Issue: `.lovable/issues/open/12-vitest-act-warnings-and-js-step-format-drift.md`
- Command precedent: `.lovable/spec/commands/06-function-size-cap-15-lines.md` (test helpers stay under the 15L cap)
- Memory: `mem://~user` (em-dash ban), `mem://preferences/test-with-features`

## Steps

1. Run `pnpm test --reporter=verbose 2>&1 | tee /tmp/vitest-full.log`; grep for `not wrapped in act` and non-zero exit assertions. Write the full inventory (file, test name, root cause category: async-mount / real-timer / prop-change / stdout-leak) to `.lovable/plans/subtasks/32-plan-10/01-vitest-inventory.md`.
2. Extract a shared `flushEffects()` + `withFakeTimers()` helper into `src/test/support/act-helpers.ts` (each function <=15 lines). Export from `src/test/support/index.ts`. Cover with a smoke test.
3. Apply the helper to every async-mount offender in Step 1's inventory (e.g. `LiveRecordedActionsTree.scroll.test.tsx`, `Popup.snapshot.test.tsx`, `Options.snapshot.test.tsx`, sibling recorder tests). Replace bare `await Promise.resolve()` and unwrapped `setTimeout` waits with `await flushEffects()` / `await act(async () => { ... })`.
4. Audit every test that uses `vi.useFakeTimers()` + `vi.advanceTimersByTime()` followed by real-timer `setTimeout`; wrap the real-timer wait in `act(async () => ...)` or migrate the whole timeline to fake timers. Document the chosen pattern in `.lovable/plans/subtasks/32-plan-10/02-timer-patterns.md`.
5. Fix the JS-step formatter contract test: assertion `Reason: JsThrew,` matches `formatFailureReport` output (already landed this turn); extend `formatFailureReport` unit tests to lock the exact separator (`, `) and add a regression test that fails if any em dash (`—` U+2014) or en dash (`–` U+2013) appears in the emitted text.
6. Add `scripts/check-em-dash-in-tests.mjs` that scans `src/**/*.{test,spec}.{ts,tsx}` and `standalone-scripts/**/*.{test,spec}.{ts,tsx}` for `—`/`–` in string literals; fail with a clear diagnostic listing every offender. Wire it into `pnpm test:preflight` and `.github/workflows/ci.yml`.
7. Silence noisy `console.log` leaks from `OptionsPage` render-branch instrumentation during tests: gate them behind `import.meta.env.MODE !== "test"` (or a `DEBUG_OPTIONS_RENDER` env flag) so `pnpm test` stdout stays clean. Verify snapshot tests still assert on rendered DOM, not console output.
8. Add a CI-side `act` warning ratchet: extend `src/test/setup.ts` to install a `console.error` spy that throws when the message starts with `Warning: An update to` or `Warning: You called act(`. Any test that trips it fails hard, preventing silent regressions. Provide an opt-out `withAllowedActWarning()` wrapper (documented, must justify use).
9. Run `pnpm test`, `pnpm run typecheck`, and `pnpm run lint --max-warnings=0` clean. Update `changelog.md` + `RELEASE_NOTES.md` with a `Testing` section describing the new helpers, the em-dash gate, and the act-warning ratchet.
10. Execute the release ceremony per `mem://workflow/release-ceremony` (minor bump — new gates count as user-visible reliability improvements), then `mv .lovable/plans/pending/32-plan-10.md .lovable/plans/completed/32-plan-10.md` and flip `Status: pending` -> `Status: completed` in the same commit as Step 10 lands.

## Verification

- `pnpm test` exits 0 with zero `act(...)` warnings in stdout and zero test failures.
- `node scripts/check-em-dash-in-tests.mjs` exits 0 (no em/en dashes in test string literals).
- `rg -n "not wrapped in act" /tmp/vitest-full.log` returns no matches after Step 3+4.
- `src/test/setup.ts` act-warning spy is exercised by a deliberately-broken fixture test (kept skipped) proving it would throw.
- Options / Popup / LiveRecordedActionsTree snapshot suites still assert their existing DOM landmarks.
- Plan file lives only in `.lovable/plans/completed/`; `pending/` has no duplicate.

## Appended from prior pending tasks

Still open under their own plans (not rolled into Plan 10):

- 11-prompts-import-export-section
- 13-per-project-chat-submit-tracker
- 22-prompt-library-test-coverage-50
- 23-prompt-library-relocate-and-light-mode
- 24-eslint-warnings-cleanup-30 (subsumed once Plan 31 Step 8 lands)
- 25-eslint-cleanup-continuation-30 (same)
- 29-version-json-single-source-of-truth
- 30-refactor-oversized-functions-15-line-cap
- 31-lint-cleanup-ctx-denylist-and-15-line-cap
