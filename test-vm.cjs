const fs = require("fs"); const vm = require("vm"); const script = fs.readFileSync("chrome-extension/projects/scripts/macro-controller/macro-looping.js", "utf8"); try { vm.runInNewContext(script, { console, setTimeout, setInterval, clearTimeout, clearInterval, document: {}, window: {}, location: {}, MutationObserver: class { observe() {} }, Event: class {} }); console.log("OK"); } catch (e) { console.error("ERR:", e); }
